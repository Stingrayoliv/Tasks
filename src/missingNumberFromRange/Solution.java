package missingNumberFromRange;

//Есть массив из неповторяющихся чисел от 1 до n (чисела вразброс).
//В массиве присутствуют все числа, кроме одного. Найти оное (это).

/**
 * the method returns the missing number from the range 1 to n
 *
 *
 * @return
 */
public class Solution {

    public int solve(int[] numbers) {
        int n=numbers.length+1;

        int completeSum=n*(n+1)/2;
        for (int i = 0; i <n-1 ; i++) {

        }
        return 0;
    }

}
